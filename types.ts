// types.ts

export interface User {
    id: number;
    name: string;
    // Add other user-related properties here
  }
  
  export interface Token {
    accessToken: string;
    // Add other token-related properties here
  }
  